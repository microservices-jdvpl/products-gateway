export enum OrderStatus {
  DELIVERED = 'DELIVERED',
  PENDING = 'PENDING',
  CANCELED = 'CANCELED',
}
export const OrderStatusList = [
  OrderStatus.DELIVERED,
  OrderStatus.PENDING,
  OrderStatus.CANCELED,
];
